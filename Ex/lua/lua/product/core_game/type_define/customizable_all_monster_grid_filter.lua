CustomizableAllMonsterGridFilter = {
    ForceMovement = 1, --只选中能被强制位移的怪物
    ForceMovementIncludeMultiSize = 2, --只选中能被强制位移的怪物(包含多格怪)
    --1 2 4 8 ……，因可能存在多个条件的与逻辑
}

_enum("CustomizableAllMonsterGridFilter", CustomizableAllMonsterGridFilter)