---@class BuffMaterialAnimationMode
local BuffMaterialAnimationMode = {
    Normal          = 0,            -- 通常
    Alternating     = 1,            -- 轮换
}

_enum("BuffMaterialAnimationMode", BuffMaterialAnimationMode)